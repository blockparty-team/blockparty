import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cphdistortion.app',
  appName: 'Distortion',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
